---
interface Props {
  href: string;
  label: string;
  iconPath: string;
  isActive?: boolean;
}

const { isActive, href, label, iconPath } = Astro.props;
---

<a
  href={href}
  class={`flex items-center text-sm sm:mb-4 ${isActive ? "sm:border-r sm:border-blue-500" : "sm:hover:border-r hover:border-blue-700"}`}
>
  <img class="w-4 h-4" src={iconPath} />
  <span class={`ml-1 font-bold ${isActive ? "text-blue-500" : ""}`}
    >{label}</span
  >
</a>
